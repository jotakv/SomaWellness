import {data,money,pct,bars} from './common.js';import {economics,annualProjection,paybackMonth,projection} from './financial-engine.js';
const m=await data('business-model');const e=economics(m), a=annualProjection(m), p=projection(m);const total=a.reduce((s,x)=>s+x.fcf,0);const vals={capital:money(m.initialCapital),capacity:m.premises.capacity,classes:m.classes.classesPerWeek,occupancy:pct(m.classes.occupancy),students:e.studentsPerClass.toLocaleString('es-ES'),workshops:m.workshops.perMonth,year1:money(a[0].fcf),stable:money(e.annualFCF),payback:`≈${Math.round(paybackMonth(m))} meses`,five:money(total),net:money(total-m.initialCapital),ticket:money(m.classes.effectiveRevenuePerAttendance),perclass:money(e.classRevenue/(m.classes.classesPerWeek*m.calendar.weeksPerMonth)),contribution:money(e.classContribution/(m.classes.classesPerWeek*m.calendar.weeksPerMonth))};document.querySelectorAll('[data-kpi]').forEach(x=>x.textContent=vals[x.dataset.kpi]);bars(document.querySelector('#fcf-chart'),p.slice(0,12).map(x=>({label:`M${x.month}`,value:x.fcf})));
